services:
  # The Backend API Service
  - type: web
    name: society-management-backend
    # Use the Dockerfile in the repository root
    dockerfilePath: Dockerfile

    # Port to expose
    envVars:
      - key: PORT
        value: "8080" # Standard Spring Boot port

      # ⭐️ DATABASE CONNECTION VARIABLES ⭐️
      - key: SPRING_DATASOURCE_URL
        value: "jdbc:postgresql://db.pximtggqmohthllmud.supabase.co:5432/postgres" # YOUR URL
      - key: SPRING_DATASOURCE_USERNAME
        value: "postgres" # Confirmed
      - key: SPRING_DATASOURCE_PASSWORD
        value: "Ramyy.17" # 🚨 REPLACE THIS WITH YOUR SUPABASE PASSWORD